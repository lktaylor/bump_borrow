<!-- Simple full-width example-->

<div class="page-header">
  <h1>Reservation #<%= @reservation.id %></h1>
</div>

<div class="row">
  <div class="col-md-12">
    <dl class="dl-horizontal">
      <dt>Reserve start</dt>
      <dd><%= @reservation.reserve_start %></dd>

      <dt>Reserve end</dt>
      <dd><%= @reservation.reserve_end %></dd>

      <dt>Item name</dt>
      <dd><%= @reservation.item.name %></dd>

      <dt></dt>
      <dd>
        <div class="btn-group">
          <a href="/reservations" class="btn btn-primary">
            <i class="fa fa-chevron-left"></i>
          </a>

          <a href="/reservations/<%= @reservation.id %>/edit" class="btn btn-warning">
            <i class="fa fa-edit"></i>
          </a>

          <a href="/delete_reservation/<%= @reservation.id %>" class="btn btn-danger">
            <i class="fa fa-trash-o"></i>
          </a>
        </div>
      </dd>
    </dl>

  </div>
</div>

<!-- End simple full-width example-->
